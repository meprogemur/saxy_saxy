#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    printf("enetr number of elements: ");
    scanf("%d", n);
    int *a = (int *)malloc(n * sizeof(int));
    int *b = (int *)malloc(n * sizeof(int));
}